import styled from "styled-components";

export const IndexWrapper = styled.main`
    display flex;
    flex-direction: row;
    width: 100%;
    max-width: 1200px;
    justify-content: center;

    padding-top: 30px;
`;
